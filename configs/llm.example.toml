# VaniScope LLM provider config example.
# Copy this file to configs/llm.local.toml for local real-provider testing.
# Do not put real API keys in this example file.

[router]
mode = "fake"
default_provider = "fake"
default_model = "fake-planner"

[budget]
max_calls_per_task = 8
max_input_tokens_per_task = 30000
max_output_tokens_per_task = 6000
max_cost_usd_per_task = 0.2
max_prompt_tokens = 30000
max_completion_tokens = 6000
max_llm_calls_per_task = 20
timeout_seconds = 60

[providers.fake]
type = "fake"
model = "fake-planner"

[providers.openai_compatible]
type = "openai_compatible"
base_url = "https://api.openai.com/v1"
api_key = "YOUR_API_KEY_HERE"
model = "gpt-4.1-mini"
timeout_seconds = 30

# Alibaba Cloud DashScope / Qwen. Set DASHSCOPE_API_KEY before selecting this
# provider in [router].
[providers.qwen]
type = "openai_compatible"
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
api_key_env = "DASHSCOPE_API_KEY"
model = "qwen-plus"
timeout_seconds = 30
